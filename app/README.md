# App Directory

This directory is reserved for user-defined routes and pages.

Currently, the framework uses templates defined in `internal/templates/` for rendering.

Future versions will support file-based routing from this directory.
